<?php  
    define("HOST", $_ENV["DB_HOST"]);     // Para o host com o qual voc� quer se conectar.
    define("USER", $_ENV["DB_USER"]);    // O nome de usu�rio para o banco de dados. 
    define("PASSWORD", $_ENV["DB_PSWD"]);    // A senha do banco de dados. 
    define("DATABASE", $_ENV["DB_NAME"]);    // O nome do banco de dados. 
?>